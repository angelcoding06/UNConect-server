version: '3'

services:
  api_gateway:
    container_name: api_gateway
    build: 
      context: .
    ports:
      - 8000:8000
    environment:
      PORT: 8000
    command: uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
